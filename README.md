# Foodgram

Foodgram — это сервис для обмена рецептами. Пользователи могут публиковать рецепты, добавлять рецепты в избранное, подписываться на авторов и скачивать список покупок с суммированными ингредиентами.

## Развёрнутый проект

- Сайт: http://icantbreathe.myftp.biz/
- Админ-панель: http://icantbreathe.myftp.biz/admin/
- Документация API: http://icantbreathe.myftp.biz/api/docs/

## Демо-аккаунты

- Администратор: `admin1@foodgram.local` / `admin123`
- Пользователь 1: `chef1@foodgram.local` / `testpass123`
- Пользователь 2: `chef2@foodgram.local` / `testpass123`

## Технологический стек

- Python
- Django
- Django REST Framework
- Djoser (токен-аутентификация)
- PostgreSQL
- Gunicorn
- Nginx
- Docker Compose
- React SPA (фронтенд)

## Структура проекта

- `backend/` — Django REST API.
- `frontend/` — одностраничное приложение на React.
- `infra/` — конфигурация Docker Compose и Nginx.
- `data/` — исходные данные ингредиентов.
- `docs/` — OpenAPI-схема и страница Redoc.

## Запуск локально

Создайте файл `backend/.env`:

```env
DEBUG=True
DJANGO_SECRET_KEY=change-me
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1
TIME_ZONE=Europe/Moscow
POSTGRES_DB=foodgram
POSTGRES_USER=foodgram_user
POSTGRES_PASSWORD=foodgram_password
DB_HOST=db
DB_PORT=5432
```

Запустите проект:

```bash
cd infra
sudo docker compose up -d --build

## Что обязательно для ревью

Да — в финальном задании для «Фудграма» **обязательно** настроить CI/CD через GitHub Actions и автоматический деплой на сервер. Это отдельный проверяемый пункт в требованиях к инфраструктуре.

Про автотесты: отдельного жёсткого требования «написать unit/integration тесты для бэкенда» в базовом чек-листе нет, но:

- рекомендуется иметь хотя бы минимальные проверки API перед сдачей (например, через Postman-коллекцию из `postman_collection/`);
- обязательно проверить, что проект полностью соответствует чек-листам по функциональности и доступам;
- если у вас в репозитории уже есть тесты, их запуск логично включить в workflow GitHub Actions вместе с линтингом.

Практический минимум перед отправкой:

## Что обязательно для ревью

Да — в финальном задании для «Фудграма» **обязательно** настроить CI/CD через GitHub Actions и автоматический деплой на сервер. Это отдельный проверяемый пункт в требованиях к инфраструктуре.

Про автотесты: отдельного жёсткого требования «написать unit/integration тесты для бэкенда» в базовом чек-листе нет, но:

- рекомендуется иметь хотя бы минимальные проверки API перед сдачей (например, через Postman-коллекцию из `postman_collection/`);
- обязательно проверить, что проект полностью соответствует чек-листам по функциональности и доступам;
- если у вас в репозитории уже есть тесты, их запуск логично включить в workflow GitHub Actions вместе с линтингом.

Практический минимум перед отправкой:

1. Прогон линтера (PEP 8).
2. Проверка ключевых сценариев по чек-листу (авторизованный/неавторизованный/админ).
3. Рабочий workflow, который собирает/проверяет проект и выполняет деплой на сервер при нужном событии (обычно push в `main`).
